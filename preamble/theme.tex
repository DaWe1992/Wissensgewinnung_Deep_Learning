% theme

% set general theme
\usetheme{CambridgeUS}
% color theme
\usecolortheme{dolphin}
% font theme
\usefonttheme{professionalfonts}
\usefonttheme[onlylarge]{structurebold}
\usefonttheme[onlymath]{serif}

% disable navigation symbols
\beamertemplatenavigationsymbolsempty

% no title on frame after framebreak
\makeatletter
\setbeamertemplate{frametitle continuation}{\gdef\beamer@frametitle{}}
\makeatother

\parskip=8pt

% linespacing
\linespread{1.2}

% background color of frame title
%\setbeamercolor{frametitle}{bg=white}

\setbeamertemplate{frametitle}{
	\nointerlineskip
    	\begin{beamercolorbox}[sep=0.1cm,ht=2.0em,wd=\paperwidth]{frametitle}
       		\usebeamerfont{frametitle}
       		\strut\insertframetitle\strut\par%
    	\end{beamercolorbox}
}

% customize bullet points
\setbeamertemplate{itemize item}[circle]
\setbeamertemplate{itemize subitem}[circle]
\setbeamertemplate{itemize subsubitem}[circle]

\setbeamertemplate{enumerate items}[circle]
\setbeamertemplate{enumerate subitem}[circle]
\setbeamertemplate{enumerate subsubitem}[circle]

% customize table of contents
\setbeamertemplate{section in toc}[circle]
\setbeamertemplate{subsection in toc}[circle]
\setbeamerfont{subsection in toc}{size=\normalsize}

% allow framebreaks per default
\let\oldframe\frame
\renewcommand\frame[1][allowframebreaks]{\oldframe[#1]}

% setup for hyperref
\hypersetup{%
	%colorlinks=true,
	linkcolor=black,
    	filecolor=magenta,
    	urlcolor=cyan,
    	pdftitle={Vertiefung Wissensgewinnung aus Daten und Deep Learning},
    	pdfauthor={Daniel Wehner},
}

\numberwithin{equation}{section}

% thickness of sum and prod symbols
\contourlength{0.2pt}

% bold sum symbol
\let\oldsum\sum
\renewcommand{\sum}{\mathop{%
	\mathchoice
      	{\text{\contour{.}{$\displaystyle\oldsum$}}}
      	{\text{\contour{.}{$\textstyle\oldsum$}}}
      	{\text{\contour{.}{$\scriptstyle\oldsum$}}}
      	{\text{\contour{.}{$\scriptscriptstyle\oldsum$}}}
}}

% bold product symbol
\let\oldprod\prod
\renewcommand{\prod}{\mathop{%
	\mathchoice
      	{\text{\contour{.}{$\displaystyle\oldprod$}}}
      	{\text{\contour{.}{$\textstyle\oldprod$}}}
      	{\text{\contour{.}{$\scriptstyle\oldprod$}}}
      	{\text{\contour{.}{$\scriptscriptstyle\oldprod$}}}
}}

% bold int symbol
\let\oldint\int
\renewcommand{\int}{\mathop{%
	\mathchoice
      	{\text{\contour{.}{$\displaystyle\oldint$}}}
      	{\text{\contour{.}{$\textstyle\oldint$}}}
      	{\text{\contour{.}{$\scriptstyle\oldint$}}}
      	{\text{\contour{.}{$\scriptscriptstyle\oldint$}}}
}}